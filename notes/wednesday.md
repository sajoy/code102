# Today I learned

- use fractions to keep body parts proportional
- how to make a function
    - ```
        var myFunction = function () {}
      ```
- if/else statements
    - ```
        if ( condition ) {
            // run this code if condition is true
        } else {
            // run this code!
        }
      ```
- how to move around in our terminals
    - `pwd`: tells where we are
    - `cd ../`: takes you to the parent dir
    - `ls ../`: list things in the parent dir